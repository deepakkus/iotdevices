{
  "name": "devices-record",
  "version": "0.1.0",
  "description": "lambda function to get the latest single record of all device ids passed",
  "main": "handler.js",
  "scripts": {
    "deploy": "serverless deploy",
    "local": "serverless invoke local --function getRecords"
  },
  "author": "Swapna",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.692.0"
  },
  "devDependencies": {
    "serverless": "^1.72.0"
  }
}
